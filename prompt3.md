I have a Cortex Agent in Snowflake that answers natural language questions about CMS Medicare Part B payment data. The agent is named MEDICARE_AGENT and is in my current database.

Please build a Streamlit in Snowflake application called MEDICARE_INSIGHTS_APP that:

1. Has a clean, professional healthcare-themed header with title "Medicare Provider Insights" and a subtitle explaining what the app does

2. Includes a text input field where users can type a natural language question

3. Calls the Cortex Agent REST API with the user's question and displays the response in a formatted output area

4. Shows a sidebar or section with 5 suggested starter questions users can click to auto-populate the input

5. Uses Snowflake brand colors (primary blue #29B5E8, dark background for headers)

Keep the code simple and focused. Create and deploy the Streamlit app directly in Snowflake.

Show me the complete app.py code before deploying so I can review it.
